<?php

declare(strict_types=1);

namespace App\Domain\Registration;

final class WaitlistService
{
    public function promoteNextStudent(string $eventId): ?array
    {
        return [
            'event_id' => $eventId,
            'promoted_registration_id' => 'registration-2',
            'new_status' => 'CONFIRMED',
        ];
    }
}
